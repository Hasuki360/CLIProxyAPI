# CLIProxyAPI Hasuki 魔改版说明

> 本仓库是基于官方 `router-for-me/CLIProxyAPI` 的专属维护与定制分支，重点解决多协议转译缺陷并提供全自动 CI/CD 构建与双机部署支持。

---

## 1. 本次修复重点（2026-09-09）

### 修复问题：Codex 请求 Google Antigravity 报 HTTP 400 失败
- **现象**：在 Codex（Desktop/CLI）中调用映射到 Antigravity（Google Gemini）账号的模型时，Google ESF 网关返回 `HTTP 400 INVALID_ARGUMENT`：
  - `'request' 中存在未知名称 "reasoning"：找不到字段`
  - `'request.service_tier' (type.googleapis.com/google.cloud.aiplatform.master.ServiceTier) 的值 "priority" 无效`
- **根因**：
  1. Codex 客户端使用 OpenAI Responses API 规范，请求体默认固化携带 `"service_tier": "priority"` 与 `"reasoning": {"effort": "..."}`。
  2. Google Antigravity（CloudCode PA）后端通过 Protobuf 严格校验，`request` 顶层没有 `reasoning` 键，且 `service_tier` 枚举不接受 OpenAI 的 `"priority"` 字符串。
  3. CLIProxyAPI 转译管线中虽然将 `reasoning.effort` 提取并映射到了 `generationConfig.thinkingConfig`，但在转给 Antigravity 时，未将外层 `service_tier` 和根级 `reasoning` 壳剥离，透传导致报错。
- **修复方案**：
  在转译层与执行器层实施多重防御性剥离：
  - `internal/translator/antigravity/openai/responses/antigravity_openai-responses_request.go`
  - `internal/translator/antigravity/gemini/antigravity_gemini_request.go`
  - `internal/translator/antigravity/openai/chat-completions/antigravity_openai_request.go`
  - `internal/runtime/executor/antigravity_executor_request.go`
  - `internal/runtime/executor/antigravity_executor_stream.go`
  - `internal/runtime/executor/antigravity_executor_execute.go`
  在组装发给 Google ESF 的负载前，自动删除 `request.service_tier`、`request.reasoning`、`service_tier`、`reasoning`。思考参数已在 `generationConfig.thinkingConfig` 中完整保留。

### 功能增强：彻底剥离与剔除 Antigravity 前置身份提示词（Identity Patch）
- **背景**：部分中转/客户端（如 Sub2API、特定 IDE 扩展）在代理 Antigravity 时，会强行向请求前置注入 `<identity>You are Antigravity, a powerful agentic AI coding assistant...</identity>` 及隔离边界。这会导致模型在酒馆、日常闲聊中出戏（自称 Antigravity、满口代码术语），并白白浪费几百 Token。
- **解决方案**：
  - 新增 `internal/util/antigravity_prompt.go`，精准识别并剥离 `<identity>`、`<communication_style>`、`[IDENTITY_PATCH]`、`Below are your system instructions...`、`--- [SYSTEM_PROMPT_END] ---` 以及老版本 `Please ignore following [ignore]` 包装；
  - 在执行器层 `geminiToAntigravity` 全协议兜底清洗，并在 Claude 转译层 `antigravity_claude_request.go` 实时过滤；
  - 若系统提示词仅包含 Antigravity 身份词，则直接剔除 `systemInstruction` 结构体；若混有真实业务提示词，则仅剥离身份词，保留真实业务指令。

---

## 2. 仓库架构与 GitHub Actions 自动更新 (方案 2)

- **GitHub 仓库**：`Hasuki360/CLIProxyAPI`
- **上游仓库**：`router-for-me/CLIProxyAPI`
- **分支规范**：
  - `upstream/main`：追踪官方最新主分支
  - `fix/codex-antigravity-service-tier`：当前稳定修复分支
- **自动构建工作流**：`.github/workflows/build-ghcr.yml`
  - 触发条件：分支推送（`main`、`fix/*`）、打 Tag（`v*`）、或手动触发（`workflow_dispatch`）。
  - 镜像发布到 GHCR：`ghcr.io/hasuki360/cli-proxy-api:latest` 与 `ghcr.io/hasuki360/cli-proxy-api:fix`。
  - Watchtower 双机监控：VPS 与本机可直接配置监控此镜像，享受免编译全自动升级。

---

## 3. 本地与 VPS 部署说明

### 3.1 本机（Hasuki-Docker）
- 原始运行目录：`/www/docker/CLIProxyAPI`（保留不动）
- 源码与开发目录：`/www/234G/CLIProxyAPI-Hasuki`
- 镜像名称：`ghcr.io/hasuki360/cli-proxy-api:latest`（或本地 `hasuki/cli-proxy-api:fix`）

### 3.2 云服务器（Huawei-VPS）
VPS 运行官方 Docker 实例，通常位于 `/www/docker/CLIProxyAPI` 或类似路径：
1. **使用 GHCR 自动更新**：
   修改 VPS 上的 `docker-compose.yml`：
   ```yaml
   services:
     cli-proxy-api:
       image: ghcr.io/hasuki360/cli-proxy-api:latest
       container_name: cli-proxy-api
       restart: always
       # ... 保留其他环境变量与挂载 ...
   ```
2. **拉取并启动**：
   ```bash
   docker compose pull && docker compose up -d
   ```
3. **Watchtower 协同**：
   Watchtower 会继续自动检测 `ghcr.io/hasuki360/cli-proxy-api:latest` 的更新并自动重启。

---

## 4. 离线快速交付命令（本地 build -> scp -> VPS load）

当不想等待 CI 或网络不通时，可直接通过本地打包交付：
```bash
# 1. 本机打包
docker save hasuki/cli-proxy-api:fix | gzip > /tmp/cli-proxy-api-fix.tar.gz

# 2. 上传到 VPS
scp /tmp/cli-proxy-api-fix.tar.gz root@vps-ip:/tmp/

# 3. VPS 载入并重启
ssh root@vps-ip "docker load -i /tmp/cli-proxy-api-fix.tar.gz && docker compose up -d"
```
